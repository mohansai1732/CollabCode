rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All room data is accessed through the authenticated Express API. Keeping
    // Firestore closed prevents a browser from bypassing membership checks.
    match /{document=**} { allow read, write: if false; }
  }
}
